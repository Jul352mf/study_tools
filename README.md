# Study Tools Repository

This repo contains two versions of the study bot:

- `messy_start/` – original prototype kept for reference.
- `Dev/` – cleaned Python package ready for further development.

See `Dev/README.md` for usage of the new version.
